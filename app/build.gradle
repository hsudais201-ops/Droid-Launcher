plugins { id 'com.android.application' }

android {
    namespace 'com.droidlauncher'
    compileSdk 37

    defaultConfig {
        applicationId 'com.droidlauncher'
        minSdk 23
        targetSdk 37
        versionCode 1
        versionName '0.1.0'
        resValue 'string', 'microsoft_client_id', project.findProperty('MICROSOFT_CLIENT_ID') ?: ''
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
